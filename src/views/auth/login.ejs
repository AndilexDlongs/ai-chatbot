<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Unisyn AI — Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            colors: { bg: { DEFAULT: '#0d0f12', soft: '#13161b' }, accent: '#8b5cf6' },
            borderRadius: { pill: '9999px' },
          },
        },
      };
    </script>
    <link rel="stylesheet" href="/css/login.css" />
  </head>
  <body class="bg-bg text-white min-h-screen flex items-center justify-center">
    <canvas id="stars" class="fixed inset-0 -z-10"></canvas>

    <div class="w-full max-w-5xl px-4 py-12 flex flex-col lg:flex-row gap-10 items-center justify-center">
      <div class="text-center lg:text-left max-w-xl">
        <div class="grid place-items-center w-16 h-16 rounded-full bg-zinc-900 ring-1 ring-purple-600/30 u-glow mb-6">
          <svg width="44" height="44" viewBox="0 0 24 24" fill="none" aria-label="Unisyn AI">
            <defs>
              <linearGradient id="uGradLogin" x1="0" y1="0" x2="1" y2="1">
                <stop offset="0" stop-color="#c4b5fd" />
                <stop offset="1" stop-color="#7c3aed" />
              </linearGradient>
            </defs>
            <path d="M6 7v5.6c0 4.1 3.4 7.4 7.5 7.4s7.5-3.3 7.5-7.4V7" stroke="url(#uGradLogin)" stroke-width="2.15" stroke-linecap="round" />
            <g class="u-orbit fast">
              <ellipse cx="12" cy="13" rx="8.6" ry="2.8" fill="none" stroke="#5a31d3" stroke-opacity=".55" stroke-width="1.6" transform="rotate(-18 12 13)" />
              <circle cx="20.2" cy="11.6" r="1.3" fill="#c4b5fd" />
            </g>
            <circle cx="14" cy="7" r="1.4" fill="#c4b5fd" />
          </svg>
        </div>
        <p class="text-sm uppercase tracking-[0.2em] text-purple-300 font-semibold mb-2">Unisyn AI</p>
        <h1 class="text-4xl font-bold mb-3">Welcome</h1>
      </div>

      <div class="w-full max-w-md">
        <div class="bg-[#11131a]/90 border border-white/10 rounded-2xl p-6 shadow-2xl backdrop-blur-lg">
          <div class="flex items-center gap-2 mb-4">
            <button id="tab-signin" class="tab-btn active">Sign in</button>
            <button id="tab-signup" class="tab-btn">Create account</button>
          </div>

          <div class="space-y-3">
            <button class="oauth-btn">
              <img src="/assets/logos/google-oauth.svg" alt="Google" class="h-5 w-5" />
              Continue with Google
            </button>
            <button class="oauth-btn">
              <img src="/assets/logos/github-oauth.svg" alt="GitHub" class="h-5 w-5" />
              Continue with GitHub
            </button>
          </div>
          
          <div class="flex items-center my-5">
            <div class="flex-1 h-px bg-white/10"></div>
            <span class="text-sm text-zinc-400 px-3">or</span>
            <div class="flex-1 h-px bg-white/10"></div>
          </div>

          <form id="authForm" class="space-y-4" data-mode="<%= typeof mode === 'string' && mode === 'signup' ? 'signup' : 'signin' %>">
            <input type="hidden" name="tier" value="<%= tier %>" />

            <label class="block text-sm text-zinc-300">
              Email
              <input type="email" name="email" required class="mt-1 w-full rounded-xl bg-[#0b0d11] border border-white/10 px-3 py-2 focus:ring-2 focus:ring-purple-600 outline-none" placeholder="you@example.com" />
            </label>

            <label class="block text-sm text-zinc-300">
              Password
              <div class="relative mt-1">
                <input type="password" name="password" id="passwordInput" required minlength="8" class="w-full rounded-xl bg-[#0b0d11] border border-white/10 px-3 py-2 pr-10 focus:ring-2 focus:ring-purple-600 outline-none" placeholder="••••••••" />
                <button type="button" id="togglePassword" class="absolute inset-y-0 right-3 flex items-center text-zinc-400 hover:text-white" aria-label="Toggle password visibility">
                  <svg id="eyeIcon" xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M2.25 12s2.25-6.75 9.75-6.75S21.75 12 21.75 12 19.5 18.75 12 18.75 2.25 12 2.25 12z" />
                    <circle cx="12" cy="12" r="3" stroke-width="1.8" />
                  </svg>
                </button>
              </div>
            </label>

            <div id="passwordRules" class="hidden bg-[#0c0e12] border border-white/5 rounded-xl p-3 text-sm space-y-2">
              <p class="text-xs text-zinc-400">Your password must contain:</p>
              <ul class="space-y-1">
                <li data-rule="length" class="rule-line text-red-400">At least 8 characters</li>
                <li data-rule="mix" class="rule-line text-red-400">At least 3 of the following:</li>
                <li class="pl-4" data-rule="lower"><span class="rule-line text-red-400">Lower case letters (a-z)</span></li>
                <li class="pl-4" data-rule="upper"><span class="rule-line text-red-400">Upper case letters (A-Z)</span></li>
                <li class="pl-4" data-rule="number"><span class="rule-line text-red-400">Numbers (0-9)</span></li>
                <li class="pl-4" data-rule="special"><span class="rule-line text-red-400">Special characters (e.g. !@#$%^&*)</span></li>
              </ul>
            </div>

            <button type="submit" class="w-full bg-accent hover:bg-purple-700 text-white font-semibold py-2.5 rounded-xl transition flex items-center justify-center gap-2">
              <span id="submitLabel">Sign in</span>
              <span id="spinner" class="hidden h-5 w-5 border-2 border-white/30 border-t-white rounded-full animate-spin"></span>
            </button>

            <div id="errorBox" class="hidden text-red-400 text-sm bg-red-900/30 border border-red-500/30 rounded-xl px-3 py-2"></div>
          </form>

        </div>
      </div>
    </div>

    <script type="module" src="/js/login.js"></script>
  </body>
</html>
